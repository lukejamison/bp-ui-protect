# UniFi Protect Configuration
# The IP address or hostname of your UniFi Protect NVR/CloudKey
NVR_IP=https://unifi-os.local

# UniFi Protect credentials
# These can be left empty if you prefer to enter them through the web interface
PROTECT_USERNAME=your_username_here
PROTECT_PASSWORD=your_password_here

# Optional: Node.js configuration
NODE_ENV=production
PORT=3000

# Optional: Allow self-signed certificates (set to 1 if your UniFi Protect uses self-signed certs)
# NODE_TLS_REJECT_UNAUTHORIZED=0
